services:
  - type: cron_job
    name: dsa-daily-generator
    env: python
    repo: https://github.com/YOUR_USERNAME/YOUR_REPO
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: python -u main.py
    schedule: "0 18 * * *"   # 11:30 PM IST
    plan: starter
